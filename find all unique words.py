def pairs-with_sum(lst,g_sum):
    complement_dict={}
     pairs=[]
    for num in 1st:
        complement=g-sum-num
        is complement in complement_dict:
            pairs.append((num,complement))
            else:
                complement_dict[num]=complement
                return pairs
            nums=[1,2,3,4,5,6,7,8,9]
            val=10
            print("list all pairs of the said list whose sum equals to",val)
            print(pairs_with.sum(nums,val))
